[project]
name = "cloud-hw1"
version = "0.1.0"
description = "Temperature conversion and average value checker"
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "flake8"
]

[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]